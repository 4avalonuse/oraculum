const finite=v=>Number.isFinite(Number(v));export function sma(rows,p){return rows.map((_,i)=>{if(i<p-1)return null;let s=0;for(let j=i-p+1;j<=i;j++)s+=Number(rows[j].c);return s/p})}export function returns(rows){return rows.map((r,i)=>i?Math.log(Number(r.c)/Number(rows[i-1].c)):null)}export function vol(rs,p=30){return rs.map((_,i)=>{const v=rs.slice(Math.max(0,i-p+1),i+1).filter(finite);if(v.length<p)return null;const m=v.reduce((a,b)=>a+b,0)/v.length;return Math.sqrt(v.reduce((a,b)=>a+(b-m)**2,0)/v.length)})}export function rsi(rows,p=14){return rows.map((_,i)=>{if(i<p)return null;let g=0,l=0;for(let j=i-p+1;j<=i;j++){const d=Number(rows[j].c)-Number(rows[j-1].c);if(d>=0)g+=d;else l-=d}return l===0?100:100-100/(1+g/l)})}export function zscore(rows,p=30){return rows.map((r,i)=>{if(i<p-1)return null;const v=rows.slice(i-p+1,i+1).map(x=>Number(x.v)).filter(finite);if(v.length<p)return null;const m=v.reduce((a,b)=>a+b,0)/v.length;const sd=Math.sqrt(v.reduce((a,b)=>a+(b-m)**2,0)/v.length);return sd?(Number(r.v)-m)/sd:0})}export function buildAnalytics(rows){const rs=returns(rows),a=sma(rows,20),b=sma(rows,100),v=vol(rs),q=rsi(rows),z=zscore(rows);return rows.map((row,i)=>({...row,return:rs[i],sma20:a[i],sma100:b[i],vol30:v[i],rsi:q[i],vz:z[i],regime:a[i]==null||b[i]==null?null:a[i]>b[i]?'bull':'bear'}))}